# External Video Archive

Video files are not committed to the Git repository. The external archive should use the same relative paths listed in `data/metadata/video_index.csv` under `archive_relative_path`.
